<!DOCTYPE html>
<html>
<head>
  <% include partials/head.ejs %>
</head>

<body>

	<% include partials/header.ejs %>
	<div>
		<p> This will be a better calendar </p>
	</div>

	<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.person.firstname %> : <%= r.day %> - <%= r.item.name %></li>
    <% }); %>
</ul>
	
</body>
</html>
